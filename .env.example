# Current reset state: Local Sample Mode
# No environment variables are required right now.
# Do not create .env.local for the reset build unless a future batch asks for it.
# Do not add real secrets, Google client IDs, service account keys, or private keys.

# Future Re-Enable: Google OAuth and Google Sheets Read-Only
# These placeholders are not required for the current reset build.
# Add values only in a future controlled re-enable batch.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_URL=
NEXTAUTH_SECRET=
